name: mpe
channels:
  - defaults
  - pytorch
  - nvidia
dependencies:
  - matplotlib
  - scikit-learn
  - tqdm
  - pip
  - pip:
    - torch
    - torchvision
    - torchaudio
    - ivon-opt==0.1
    - laplace-torch==0.1a2
